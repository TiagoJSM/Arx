using System;
using UnityEngine;

namespace QuestSystem
{
	public delegate void OnKill(GameObject obj);
	public delegate void OnInventoryAdd(GameObject obj);
	public delegate void OnInventoryRemove(GameObject obj);
}

